name: ct312h_project
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^13.0.0
  sqflite: ^2.3.2
  path: ^1.9.0
  flutter_local_notifications: ^17.0.0
  http: ^1.2.0
  provider: ^6.1.5
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

  # Nếu sau này bạn có dùng hình ảnh hay font chữ, hãy thêm ở đây:
  # assets:
  #   - assets/images/
